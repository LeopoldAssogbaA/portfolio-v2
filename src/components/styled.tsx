import styled from "styled-components";

export const MainContainer = styled.section`
  background: transparent;
  width: 100vw;
  height: 500vh;
  position: relative;
  .school {
    display: none;
  }
`;